[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "grandalf>=0.8",
    "langchain[anthropic,openai]>=1.3.15",
    "langgraph>=1.2.11",
    "pydantic-settings>=2.15.0",
]
