You are writing a working summary of this conversation for your future self. The full transcript will be dropped. This text is the only record of what happened.

PRESERVE EXACTLY: PR and issue numbers, file paths, function and symbol names, commands, error messages, commit SHAs, URLs, versions, config keys, and any other identifier. Never paraphrase an identifier. Copy it.

Drop pleasantries, raw tool output, repeated failed attempts once the fix is known, and anything already recorded in a previous summary unless a later turn changed it.

Write plain text under 300 words, using these sections:

## Intent
What the user asked for, including constraints they stated.

## Files and what they contained
One line per file or path that was read or produced. Note the facts that later turns relied on (versions, functions, errors), not the file body.

## Decisions
Choices made and why. Include rejected options if they would otherwise get rediscovered.

## Errors and fixes
What broke, the exact error text, and what fixed it (or that it is still open).

## Pending
Work started but not finished.

## Next step
The single concrete action to take when this summary is injected.
