# Memory is one facade over feeds proven in order

Core memory callers depend on one facade for recall, open, remember, and forget. Files, the markdown knowledge graph, and any later transcript index remain feeds behind that interface. kinby proves each feed with expected-behavior checks before adding the next, so the graph starts as user-readable markdown rather than a database or retrieval index whose value the memory evals have not shown.
